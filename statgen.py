import random

def generate(instats, inlevel, racestats, statpref):
	#Should this take into account classes that don't want to dump all their stat points in one stat?

	#First, find out the race's stat bonuses/minuses

	#Next, generate 6 stat numbers (currently 4d6 drop lowest)

	#Then, assign stats based on class priority list

	#Finally, assign racial and level bonuses

	return "Just testing for now!"